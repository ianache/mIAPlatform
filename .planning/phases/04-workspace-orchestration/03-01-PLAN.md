---
phase: 03-workspace-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/pages/WorkspacePage.vue
  - src/frontend/src/components/FloatingPromptBar.vue
  - src/frontend/src/components/AgentActivityZone.vue
  - src/frontend/src/components/AgentActivityCard.vue
  - src/frontend/src/composables/useAgentExecution.ts
  - src/frontend/src/api/workspace.ts
autonomous: true
requirements: [WORKSPACE-01, WORKSPACE-02, WORKSPACE-03]
user_setup: []

must_haves:
  truths:
    - "User sees a floating prompt bar at the bottom center of the workspace"
    - "User can type a prompt and send it to trigger agent execution"
    - "Agent activity zone shows active agents with status (thinking, acting, idle)"
    - "Agent cards display avatar, name, current action, and progress"
    - "Execution results appear in the main working area"
  artifacts:
    - path: "src/frontend/src/components/FloatingPromptBar.vue"
      provides: "Glassmorphic floating input bar with send button"
      exports: ["FloatingPromptBar"]
    - path: "src/frontend/src/components/AgentActivityZone.vue"
      provides: "Right sidebar showing active agent cards"
      exports: ["AgentActivityZone"]
    - path: "src/frontend/src/components/AgentActivityCard.vue"
      provides: "Individual agent status card with animations"
      exports: ["AgentActivityCard"]
    - path: "src/frontend/src/composables/useAgentExecution.ts"
      provides: "Composition function for agent execution lifecycle"
      exports: ["useAgentExecution"]
  key_links:
    - from: "FloatingPromptBar"
      to: "useAgentExecution"
      via: "emit('submit', prompt) -> execute()"
    - from: "AgentActivityZone"
      to: "WebSocket"
      via: "ws_manager.py events -> agent status updates"
    - from: "useAgentExecution"
      to: "workspace API"
      via: "POST /api/v1/workspace/subprojects/{id}/executions"
---

<objective>
Build the workspace dashboard UI with floating prompt bar and agent activity zone.

Purpose: This is the main user interface for multi-agent orchestration. Users interact with agents through the floating prompt bar and monitor agent activity in real-time through the activity zone.
Output: Complete workspace UI matching the multi_agent_ai_dashboard spec with glassmorphism styling.
</objective>

<execution_context>
@C:/Users/ianache/Desktop/DATA/01-DOCUMENTOS/02-PROYECTOS/104-mIAPlatform/mIAPlatform/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/Desktop/DATA/01-DOCUMENTOS/02-PROYECTOS/104-mIAPlatform/mIAPlatform/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@00-SPECIFICATION/multi_agent_ai_dashboard/code.html
@src/frontend/src/pages/WorkspacePage.vue
@src/frontend/src/api/client.ts
@src/backend/api/workspace.py
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From src/backend/api/workspace.py:
```python
@router.post("/subprojects/{subproject_id}/executions", response_model=AgentExecutionResponse)
async def create_execution(subproject_id: UUID, execution_in: AgentExecutionCreate, ...)
```

From src/frontend/src/api/client.ts (pattern):
```typescript
export const api = {
  get: (url: string) => fetch(url, { headers: authHeaders() }),
  post: (url: string, data: any) => fetch(url, { method: 'POST', body: JSON.stringify(data), headers: authHeaders() }),
}
```

Design System (from REQUIREMENTS.md):
- Background: #091421
- Primary: #ADC6FF
- Surface Container: #16202E
- Surface Container High: #212B39
- Glass effect: rgba(48, 58, 72, 0.6) with blur(12px)
- Fonts: Space Grotesk (headlines), Manrope (body), Inter (labels)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create FloatingPromptBar component</name>
  <files>src/frontend/src/components/FloatingPromptBar.vue</files>
  <read_first>
    - src/frontend/src/pages/WorkspacePage.vue (existing workspace layout)
    - 00-SPECIFICATION/multi_agent_ai_dashboard/code.html (design reference)
  </read_first>
  <acceptance_criteria>
    - Component file exists at src/frontend/src/components/FloatingPromptBar.vue
    - Uses glass-panel class with backdrop-filter: blur(12px)
    - Background is rgba(48, 58, 72, 0.6) per design system
    - Positioned fixed at bottom center (bottom: 2rem, left: 50%, transform: translateX(-50%))
    - Width: min(600px, 90vw) for responsive design
    - Contains textarea for multiline input with auto-resize
    - Send button with primary gradient (from-primary to-primary-container)
    - Emits 'submit' event with prompt text when send clicked or Ctrl+Enter pressed
    - Has placeholder text: "Type your command or question..."
    - Material Icons send icon (arrow_upward or send)
  </acceptance_criteria>
  <action>
    Create FloatingPromptBar.vue as a glassmorphic floating input bar:
    - Use Vue 3 composition API with defineProps/defineEmits
    - Template: glass panel container with textarea and circular send button
    - Style: position fixed, centered at bottom, z-index 50
    - Textarea: rows=1 auto-expanding to max 5 rows, dark background (#121C2A)
    - Send button: positioned absolute right, circular, primary gradient
    - Emit 'submit' event with trimmed prompt value
    - Handle keyboard: Ctrl+Enter submits
    - Clear input after successful submit
    - Add subtle shadow and border matching design system
  </action>
  <verify>
    <automated>grep -l "FloatingPromptBar" src/frontend/src/components/FloatingPromptBar.vue && grep "emit('submit'" src/frontend/src/components/FloatingPromptBar.vue</automated>
  </verify>
  <done>FloatingPromptBar component exists with glassmorphism styling, positioned correctly, emits submit event</done>
</task>

<task type="auto">
  <name>Task 2: Create AgentActivityCard component</name>
  <files>src/frontend/src/components/AgentActivityCard.vue</files>
  <read_first>
    - src/frontend/src/components/AgentCard.vue (existing agent card for reference)
    - 00-SPECIFICATION/multi_agent_ai_dashboard/code.html
  </read_first>
  <acceptance_criteria>
    - Component file exists at src/frontend/src/components/AgentActivityCard.vue
    - Props: agent (object with name, avatar_url, status), currentAction (string), progress (number 0-100)
    - Status values: 'idle', 'thinking', 'acting', 'error'
    - Shows agent avatar (circular, 40px) with online/offline indicator dot
    - Displays agent name in Space Grotesk font
    - Shows current action text in smaller font (e.g., "Processing data...")
    - Progress bar visible when status is 'thinking' or 'acting'
    - Status indicator with pulsing animation when active
    - Card background: surface-container-high (#212B39)
    - Card has subtle border radius (0.5rem) and padding
  </acceptance_criteria>
  <action>
    Create AgentActivityCard.vue for individual agent status display:
    - Props interface: { agent: Agent, status: 'idle'|'thinking'|'acting'|'error', currentAction?: string, progress?: number }
    - Template: flexbox row with avatar, info column, status indicator
    - Avatar: circular image with 2px border in primary color, status dot overlay
    - Status dot: green (idle), blue pulsing (thinking/acting), red (error)
    - Info section: agent name (font-headline), action text (font-body, text-on-surface-variant)
    - Progress bar: horizontal bar with primary gradient fill, only show when progress > 0
    - Add CSS animations: pulse for active status, smooth progress transitions
    - Use design system colors exclusively
  </action>
  <verify>
    <automated>grep -l "AgentActivityCard" src/frontend/src/components/AgentActivityCard.vue && grep "status.*thinking\|acting\|idle\|error" src/frontend/src/components/AgentActivityCard.vue</automated>
  </verify>
  <done>AgentActivityCard component displays agent with status, action text, and animated progress</done>
</task>

<task type="auto">
  <name>Task 3: Create AgentActivityZone component</name>
  <files>src/frontend/src/components/AgentActivityZone.vue</files>
  <read_first>
    - src/frontend/src/components/AgentActivityCard.vue (created in Task 2)
    - 00-SPECIFICATION/multi_agent_ai_dashboard/code.html
  </read_first>
  <acceptance_criteria>
    - Component file exists at src/frontend/src/components/AgentActivityZone.vue
    - Props: agents (array), executions (array of active executions)
    - Renders list of AgentActivityCard components
    - Section header: "Active Agents" with count badge
    - When no agents active: shows "No active agents" message with illustration
    - Updates in real-time as agents become active/idle
    - Scrollable container with custom scrollbar styling
    - Width: fixed 320px, full height of parent
    - Background: surface-container (#16202E)
    - Border-left: 1px solid outline-variant color
  </acceptance_criteria>
  <action>
    Create AgentActivityZone.vue as the right sidebar:
    - Props: { agents: Agent[], activeExecutions: Execution[] }
    - Computed: Map executions to agent activity status
    - Template: container with header and scrollable list
    - Header: "Active Agents" title + badge with count of non-idle agents
    - List: v-for over agents, render AgentActivityCard for each
    - Sort: Active agents (thinking/acting) first, then idle
    - Empty state: Centered message with icon when no active agents
    - Styling: fixed width 320px, full height, overflow-y-auto with custom scrollbar
    - Scrollbar: 4px width, track transparent, thumb #2b3544
  </action>
  <verify>
    <automated>grep -l "AgentActivityZone" src/frontend/src/components/AgentActivityZone.vue && grep "AgentActivityCard" src/frontend/src/components/AgentActivityZone.vue && grep "activeExecutions" src/frontend/src/components/AgentActivityZone.vue</automated>
  </verify>
  <done>AgentActivityZone component renders sidebar with active agent cards, handles empty state</done>
</task>

<task type="auto">
  <name>Task 4: Create useAgentExecution composable</name>
  <files>src/frontend/src/composables/useAgentExecution.ts, src/frontend/src/api/workspace.ts</files>
  <read_first>
    - src/backend/api/workspace.py (execution endpoints)
    - src/frontend/src/api/client.ts (API pattern)
  </read_first>
  <acceptance_criteria>
    - Composable file exists at src/frontend/src/composables/useAgentExecution.ts
    - Returns: { execute, isExecuting, error, executionId, connectToExecution, executionEvents }
    - execute(subprojectId: string, prompt: string) triggers POST to /api/v1/workspace/subprojects/{id}/executions
    - WebSocket connection to /ws/executions/{executionId} for real-time updates
    - Execution events captured: 'step', 'thought', 'action', 'artifact', 'complete', 'error'
    - Proper cleanup on component unmount (close WebSocket)
    - Error handling with user-friendly messages
    - TypeScript interfaces for Execution, ExecutionEvent
  </acceptance_criteria>
  <action>
    Create useAgentExecution composable for agent execution lifecycle:
    - Reactive state: isExecuting (boolean), error (string|null), executionId (string|null), events (array)
    - execute() function: 
      - POST to /api/v1/workspace/subprojects/{subprojectId}/executions with { prompt }
      - Set executionId from response
      - Connect WebSocket to receive events
      - Handle HTTP errors (404, 400, etc.) with descriptive messages
    - WebSocket connection:
      - URL: ws://localhost:8000/ws/executions/{executionId}?token={jwt}
      - Message handlers for each event type
      - Push events to reactive events array
      - Auto-close on 'complete' or 'error' event
    - Cleanup: onUnmounted close WebSocket if open
    - Types: Define ExecutionEvent interface with type, data, timestamp fields
  </action>
  <verify>
    <automated>grep -l "useAgentExecution" src/frontend/src/composables/useAgentExecution.ts && grep "WebSocket\|ws://" src/frontend/src/composables/useAgentExecution.ts && grep "execute.*subprojectId.*prompt" src/frontend/src/composables/useAgentExecution.ts</automated>
  </verify>
  <done>useAgentExecution composable handles full execution lifecycle with WebSocket updates</done>
</task>

<task type="auto">
  <name>Task 5: Integrate components into WorkspacePage</name>
  <files>src/frontend/src/pages/WorkspacePage.vue</files>
  <read_first>
    - src/frontend/src/pages/WorkspacePage.vue (current implementation)
    - src/frontend/src/components/FloatingPromptBar.vue
    - src/frontend/src/components/AgentActivityZone.vue
    - src/frontend/src/composables/useAgentExecution.ts
  </read_first>
  <acceptance_criteria>
    - WorkspacePage imports and uses FloatingPromptBar
    - WorkspacePage imports and uses AgentActivityZone
    - WorkspacePage uses useAgentExecution composable
    - Layout: main content area + right sidebar (AgentActivityZone)
    - FloatingPromptBar fixed at bottom center
    - On prompt submit: calls execute() from composable
    - Execution events displayed in main content area
    - Agent activity reflects execution state
    - Responsive: sidebar collapsible on mobile (< 768px)
  </acceptance_criteria>
  <action>
    Update WorkspacePage.vue to integrate new components:
    - Add imports: FloatingPromptBar, AgentActivityZone, useAgentExecution
    - Setup: const { execute, isExecuting, error, executionId, events } = useAgentExecution()
    - Template structure:
      - Main container: flex row
      - Left/main area: execution results display (scrollable)
      - Right sidebar: AgentActivityZone component
      - Bottom: FloatingPromptBar
    - Handle submit event from FloatingPromptBar:
      - Get current subproject ID from route or state
      - Call execute(subprojectId, prompt)
      - Show loading state
    - Display execution events in main area:
      - Create timeline of events from composable
      - Show thinking steps, actions, artifacts
    - Pass active executions to AgentActivityZone
    - Add responsive breakpoints: hide sidebar on mobile, show toggle button
  </action>
  <verify>
    <automated>grep "FloatingPromptBar" src/frontend/src/pages/WorkspacePage.vue && grep "AgentActivityZone" src/frontend/src/pages/WorkspacePage.vue && grep "useAgentExecution" src/frontend/src/pages/WorkspacePage.vue</automated>
  </verify>
  <done>WorkspacePage integrates all components, handles execution flow, responsive layout</done>
</task>

</tasks>

<verification>
- FloatingPromptBar renders as glassmorphic bar at bottom center
- AgentActivityZone renders as right sidebar with agent cards
- AgentActivityCard shows status, action, progress per agent
- useAgentExecution connects to backend API and WebSocket
- WorkspacePage integrates all components cohesively
- Submitting a prompt triggers execution and shows results
- Design matches multi_agent_ai_dashboard spec
</verification>

<success_criteria>
- User can type prompt in floating bar and submit with Ctrl+Enter
- Agent activity zone shows real-time agent status
- Execution results display in main working area
- UI matches design system (colors, fonts, glassmorphism)
- Responsive layout works on different screen sizes
- No console errors during execution flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-workspace-orchestration/03-01-SUMMARY.md`
</output>
