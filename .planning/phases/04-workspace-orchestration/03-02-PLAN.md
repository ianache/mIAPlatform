---
phase: 03-workspace-orchestration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/orchestration/adk_engine.py
  - src/backend/orchestration/adk_agents.py
  - src/backend/orchestration/multi_agent_runner.py
  - src/backend/models/adk_schemas.py
  - src/backend/api/orchestration.py
  - src/backend/core/config.py
  - pyproject.toml
autonomous: true
requirements: [WORKSPACE-04, WORKSPACE-05, WORKSPACE-06]
user_setup: []

must_haves:
  truths:
    - "Google ADK is installed and configured as orchestration engine"
    - "Multi-agent sessions can be created with a primary orchestrator agent"
    - "Sub-agents can be dynamically loaded and invoked by the orchestrator"
    - "Agent execution flow: User prompt -> ADK orchestrator -> Sub-agents -> Response"
    - "ADK events are captured and streamed via WebSocket to frontend"
  artifacts:
    - path: "src/backend/orchestration/adk_engine.py"
      provides: "ADK session management and event loop"
      exports: ["ADKEngine", "create_session", "run_agent_turn"]
    - path: "src/backend/orchestration/adk_agents.py"
      provides: "Orchestrator agent and sub-agent definitions"
      exports: ["OrchestratorAgent", "create_sub_agent"]
    - path: "src/backend/orchestration/multi_agent_runner.py"
      provides: "High-level runner for multi-agent execution"
      exports: ["MultiAgentRunner", "run_multi_agent_session"]
    - path: "src/backend/models/adk_schemas.py"
      provides: "Pydantic schemas for ADK data structures"
      exports: ["ADKSessionConfig", "AgentInvocation", "ADKEvent"]
  key_links:
    - from: "workspace.py executions endpoint"
      to: "MultiAgentRunner"
      via: "runner.run(session_config, prompt)"
    - from: "MultiAgentRunner"
      to: "ADKEngine"
      via: "engine.run_agent_turn(session, orchestrator)"
    - from: "ADKEngine"
      to: "WebSocket"
      via: "event_manager.broadcast(event)"
---

<objective>
Integrate Google ADK for multi-agent orchestration, enabling dynamic agent collaboration with a primary orchestrator coordinating sub-agents.

Purpose: Google ADK provides the orchestration layer for multi-agent systems. The orchestrator agent receives user prompts, decides which sub-agents to invoke, and coordinates their execution to produce a unified response.
Output: Complete ADK integration with orchestrator pattern, sub-agent loading, and event streaming.
</objective>

<execution_context>
@C:/Users/ianache/Desktop/DATA/01-DOCUMENTOS/02-PROYECTOS/104-mIAPlatform/mIAPlatform/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/Desktop/DATA/01-DOCUMENTOS/02-PROYECTOS/104-mIAPlatform/mIAPlatform/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/backend/api/workspace.py
@src/backend/llm/gateway.py
@src/backend/models/agent.py
@src/backend/models/workspace.py
</context>

<interfaces>
From src/backend/models/agent.py:
```python
class Agent(Base):
    id = Column(PG_UUID(as_uuid=True), primary_key=True, default=uuid4)
    tenant_id = Column(String, index=True, nullable=False)
    name = Column(String, nullable=False)
    provider = Column(String, nullable=False)
    model = Column(String, nullable=False)
    temperature = Column(Float, default=0.7)
    system_prompt = Column(Text, nullable=True)
    capabilities = Column(JSON, default=list)
    status = Column(String, default="ready")
```

From src/backend/llm/gateway.py:
```python
async def complete(
    provider: str,
    model: str,
    messages: List[Dict[str, str]],
    temperature: float = 0.7,
    max_tokens: int = 1000,
    stream: bool = False,
) -> ModelResponse:
```

Google ADK Pattern:
- Agent class with name, model, instruction, tools
- Runner manages session and execution loop
- Events emitted: turn_start, turn_end, tool_call, tool_result, error
- Session maintains state across turns
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure Google ADK</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (existing dependencies)
  </read_first>
  <acceptance_criteria>
    - "google-adk" added to pyproject.toml dependencies
    - Version constraint: >=0.1.0 (latest stable)
    - ADK imports work without errors: from google.adk import Agent, Runner
    - No dependency conflicts with existing packages
  </acceptance_criteria>
  <action>
    Add Google ADK to project dependencies:
    - Open pyproject.toml
    - Add "google-adk>=0.1.0" to dependencies list
    - Run `uv pip install google-adk` to verify installation
    - Test import: python -c "from google.adk import Agent, Runner; print('ADK OK')"
    - Resolve any version conflicts if they arise
  </action>
  <verify>
    <automated>grep "google-adk" pyproject.toml && python -c "from google.adk import Agent; print('ADK import OK')" 2>/dev/null || echo "Install verification needed"</automated>
  </verify>
  <done>Google ADK installed and importable, no dependency conflicts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ADK schemas and types</name>
  <files>src/backend/models/adk_schemas.py</files>
  <read_first>
    - src/backend/models/agent_schemas.py (existing pattern)
  </read_first>
  <acceptance_criteria>
    - File exists at src/backend/models/adk_schemas.py
    - ADKSessionConfig: session_id, tenant_id, subproject_id, agent_ids (list), orchestrator_config
    - AgentInvocation: agent_id, name, prompt, context, tools
    - ADKEvent: event_type (enum), agent_id, data, timestamp
    - Event types: 'turn_start', 'turn_end', 'agent_invoked', 'tool_call', 'tool_result', 'thought', 'response', 'error'
    - All schemas use Pydantic v2 with proper validation
    - Tests pass for schema validation
  </acceptance_criteria>
  <behavior>
    - Test 1: ADKSessionConfig validates with valid UUIDs and agent list
    - Test 2: ADKEvent rejects invalid event_type
    - Test 3: AgentInvocation serializes tools correctly
    - Test 4: Schema defaults work (temperature defaults to 0.7)
  </behavior>
  <action>
    Create ADK schemas:
    - ADKSessionConfig: Pydantic model with session_id (UUID), tenant_id (str), subproject_id (UUID), agent_ids (List[UUID]), orchestrator_agent_id (UUID), context (dict)
    - AgentInvocation: agent_id (UUID), name (str), prompt (str), system_prompt (str), tools (List[dict]), model_config (dict)
    - ADKEventType: Literal enum with all event types
    - ADKEvent: event_type (ADKEventType), agent_id (Optional[UUID]), data (dict), timestamp (datetime), metadata (Optional[dict])
    - Create tests in tests/test_adk_schemas.py
  </action>
  <verify>
    <automated>pytest tests/test_adk_schemas.py -x -v</automated>
  </verify>
  <done>ADK schemas created with validation, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ADK agent definitions</name>
  <files>src/backend/orchestration/adk_agents.py</files>
  <read_first>
    - src/backend/models/agent.py (agent model structure)
    - src/backend/llm/gateway.py (LLM integration)
  </read_first>
  <acceptance_criteria>
    - File exists at src/backend/orchestration/adk_agents.py
    - OrchestratorAgent class extends ADK Agent
    - Orchestrator has system prompt defining sub-agent coordination role
    - create_sub_agent() function converts DB Agent model to ADK Agent
    - Sub-agents inherit configuration from DB (model, temperature, system_prompt)
    - Tool registry for agent capabilities (web_search, file_analysis, etc.)
  </acceptance_criteria>
  <behavior>
    - Test 1: create_sub_agent with valid Agent model returns ADK Agent
    - Test 2: OrchestratorAgent invokes sub-agents based on prompt
    - Test 3: Sub-agent tools are registered and callable
    - Test 4: Agent configuration (temp, model) flows from DB to ADK
  </behavior>
  <action>
    Create ADK agent wrappers:
    - OrchestratorAgent class:
      - System prompt: "You are the primary orchestrator. Analyze user requests and delegate to appropriate sub-agents. Coordinate their responses into a unified answer."
      - Model: Use gateway for LLM calls
      - Tools: delegate_to_agent(agent_id, prompt) function
    - create_sub_agent(agent_model: Agent) -> Agent:
      - Map DB fields to ADK Agent constructor
      - name=agent_model.name, model=agent_model.model
      - instruction=agent_model.system_prompt
      - tools=map_capabilities_to_tools(agent_model.capabilities)
    - Tool mappings:
      - web_search -> search_web(query)
      - file_analysis -> analyze_file(file_path)
      - code_execution -> execute_code(code, language)
    - Store agent registry (agent_id -> ADK Agent) for runtime lookup
  </action>
  <verify>
    <automated>pytest tests/test_adk_agents.py -x -v</automated>
  </verify>
  <done>ADK agent wrappers created, sub-agent creation works, tools mapped</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create ADK engine with event streaming</name>
  <files>src/backend/orchestration/adk_engine.py</files>
  <read_first>
    - src/backend/api/ws_manager.py (WebSocket event manager)
    - src/backend/orchestration/adk_agents.py
  </read_first>
  <acceptance_criteria>
    - File exists at src/backend/orchestration/adk_engine.py
    - ADKEngine class manages ADK Runner instances
    - create_session() initializes new session with agent registry
    - run_agent_turn() executes one turn with event capture
    - Events are emitted to WebSocket via AgentEventManager
    - Session state persists across turns
    - Proper error handling with event emission on failures
  </acceptance_criteria>
  <behavior>
    - Test 1: create_session returns valid ADK session
    - Test 2: run_agent_turn emits turn_start and turn_end events
    - Test 3: Tool calls emit tool_call and tool_result events
    - Test 4: Errors emit error event with details
    - Test 5: Session maintains conversation history
  </behavior>
  <action>
    Create ADK engine:
    - ADKEngine class:
      - __init__: Initialize with event_manager reference
      - create_session(session_config: ADKSessionConfig) -> Session:
        - Create InMemorySessionService
        - Load orchestrator and sub-agents from config
        - Store in session state
      - run_agent_turn(session_id: str, prompt: str) -> AsyncIterator[ADKEvent]:
        - Get session and runner
        - Emit turn_start event
        - Run agent with prompt
        - Capture ADK events and convert to ADKEvent schema
        - Emit each event via event_manager
        - Yield events for streaming
        - Emit turn_end with final response
    - Event conversion:
      - Map ADK native events to our ADKEvent schema
      - Include agent_id attribution where available
    - Error handling:
      - Try/except around agent execution
      - Emit error event on exception
      - Log errors with context
  </action>
  <verify>
    <automated>pytest tests/test_adk_engine.py -x -v</automated>
  </verify>
  <done>ADK engine created with session management and event streaming</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create multi-agent runner</name>
  <files>src/backend/orchestration/multi_agent_runner.py</files>
  <read_first>
    - src/backend/orchestration/adk_engine.py
    - src/backend/models/workspace.py (executions)
  </read_first>
  <acceptance_criteria>
    - File exists at src/backend/orchestration/multi_agent_runner.py
    - MultiAgentRunner class orchestrates full execution flow
    - run_multi_agent_session() is the main entry point
    - Integrates with workspace executions (creates execution record)
    - Streams events via WebSocket during execution
    - Updates execution status (running -> success/failed)
    - Returns final execution result with artifacts
  </acceptance_criteria>
  <behavior>
    - Test 1: run_multi_agent_session creates execution record
    - Test 2: Events stream to WebSocket during execution
    - Test 3: Execution status updates correctly
    - Test 4: Final result contains response and artifacts
    - Test 5: Failed executions update status and capture error
  </behavior>
  <action>
    Create multi-agent runner:
    - MultiAgentRunner class:
      - __init__: db_session, event_manager, adk_engine
      - async run(session_config: ADKSessionConfig, prompt: str) -> ExecutionResult:
        - Create execution record in DB (status='running')
        - Set up ADK session via engine
        - Subscribe to events and broadcast to WebSocket
        - Run agent turn with prompt
        - Collect all events and final response
        - Update execution record (status='success', final_output)
        - Persist events as execution steps/actions
        - Return ExecutionResult with response and metadata
    - Integration with workspace API:
      - Import and use in workspace.py execution endpoints
      - Replace simple execution with multi-agent flow
    - Error handling:
      - Catch exceptions, update execution status='failed'
      - Capture error details in execution record
      - Emit error event to WebSocket
  </action>
  <verify>
    <automated>pytest tests/test_multi_agent_runner.py -x -v</automated>
  </verify>
  <done>Multi-agent runner created, integrates with workspace executions</done>
</task>

<task type="auto">
  <name>Task 6: Create orchestration API endpoints</name>
  <files>src/backend/api/orchestration.py</files>
  <read_first>
    - src/backend/api/workspace.py (pattern for API routes)
    - src/backend/orchestration/multi_agent_runner.py
  </read_first>
  <acceptance_criteria>
    - File exists at src/backend/api/orchestration.py
    - POST /api/v1/orchestration/sessions - Create new orchestration session
    - POST /api/v1/orchestration/sessions/{id}/run - Execute prompt in session
    - GET /api/v1/orchestration/sessions/{id} - Get session status
    - DELETE /api/v1/orchestration/sessions/{id} - End session
    - All endpoints tenant-isolated
    - Router registered in main.py
  </acceptance_criteria>
  <action>
    Create orchestration API:
    - Router with prefix="/orchestration", tags=["orchestration"]
    - Endpoints:
      - POST /sessions: Create ADK session with agent configuration
        - Body: { subproject_id, agent_ids, orchestrator_config }
        - Returns: { session_id, status, agents_loaded }
      - POST /sessions/{session_id}/run: Execute prompt
        - Body: { prompt }
        - Triggers MultiAgentRunner
        - Returns: { execution_id, status }
      - GET /sessions/{session_id}: Get session details
        - Returns: { session_id, status, agents, execution_count }
      - DELETE /sessions/{session_id}: Clean up session
        - Returns: 204 No Content
    - Tenant isolation: Extract tenant_id from JWT, validate access
    - Register router in main.py: app.include_router(orchestration.router, prefix="/api/v1")
  </action>
  <verify>
    <automated>grep "orchestration" src/backend/main.py && grep -E "POST|GET|DELETE" src/backend/api/orchestration.py | head -10</automated>
  </verify>
  <done>Orchestration API endpoints created and registered</done>
</task>

</tasks>

<verification>
- Google ADK installed and importable
- ADK schemas validate correctly
- Orchestrator agent can be created with system prompt
- Sub-agents load from DB Agent models
- ADK engine creates sessions and runs turns
- Events stream via WebSocket during execution
- Multi-agent runner integrates with workspace executions
- API endpoints work and are tenant-isolated
</verification>

<success_criteria>
- ADK session can be created via API
- User prompt triggers orchestrator -> sub-agent delegation
- Real-time events visible in frontend via WebSocket
- Execution record created and updated correctly
- Multi-agent collaboration produces unified response
- Tenant isolation enforced (A can't see B's sessions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-workspace-orchestration/03-02-SUMMARY.md`
</output>
