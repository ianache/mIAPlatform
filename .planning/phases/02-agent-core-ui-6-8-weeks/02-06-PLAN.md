---
phase: 02-agent-core-ui-6-8-weeks
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pyproject.toml
  - src/backend/core/config.py
  - docker-compose.yml
  - .env.example
autonomous: false
requirements: [KEYCLOAK-01, UV-01]
user_setup:
  - service: keycloak
    why: "Keycloak v26 must be configured on external host oauth2.qa.comsatel.com.pe"
    dashboard_config:
      - task: "Verify Realm 'Apps' exists on oauth2.qa.comsatel.com.pe"
        location: "Keycloak Admin Console -> Realms -> Apps"
      - task: "Verify Client ID 'miaplatform' is configured with correct redirect URIs"
        location: "Keycloak Admin Console -> Clients -> miaplatform"
      - task: "Create test user in realm for authentication testing"
        location: "Keycloak Admin Console -> Users -> Create user"
    env_vars:
      - name: KEYCLOAK_URL
        source: "https://oauth2.qa.comsatel.com.pe"
      - name: KEYCLOAK_REALM
        source: "Apps"
      - name: KEYCLOAK_CLIENT_ID
        source: "miaplatform"
      - name: KEYCLOAK_CLIENT_SECRET
        source: "Keycloak Admin Console -> Clients -> miaplatform -> Credentials"

must_haves:
  truths:
    - "pyproject.toml exists with all dependencies from requirements.txt"
    - "uv can install all project dependencies from pyproject.toml"
    - "Keycloak configuration points to oauth2.qa.comsatel.com.pe realm Apps"
    - "docker-compose.yml includes Keycloak v26 service"
    - ".env.example documents all Keycloak env vars"
  artifacts:
    - path: "pyproject.toml"
      provides: "Python project config with uv-compatible dependency declarations"
      contains: "[project]"
    - path: "src/backend/core/config.py"
      provides: "Updated Keycloak settings pointing to production QA host"
      contains: "oauth2.qa.comsatel.com.pe"
    - path: "docker-compose.yml"
      provides: "Keycloak v26 service definition"
      contains: "keycloak"
  key_links:
    - from: "src/backend/core/config.py"
      to: "oauth2.qa.comsatel.com.pe"
      via: "KEYCLOAK_URL setting"
      pattern: "KEYCLOAK_URL.*oauth2"
---

<objective>
Configure Keycloak v26 connection and migrate Python package management from requirements.txt to uv/pyproject.toml.

Purpose: Infrastructure tasks that don't depend on other Phase 2 work but are required for Phase 2 deliverables. Keycloak must point to the real QA host, and uv replaces requirements.txt for modern Python dependency management.
Output: pyproject.toml, updated Keycloak config, docker-compose with Keycloak v26, documented env vars.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 2 tasks 9-10)
@.planning/REQUIREMENTS.md (Phase 2: Keycloak v26 + uv migration)
@src/backend/core/config.py
@requirements.txt
@docker-compose.yml
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Python dependencies to uv/pyproject.toml</name>
  <files>pyproject.toml, requirements.txt</files>
  <action>
    Create pyproject.toml with [project] section:
    - name = "orchestra-backend"
    - version = "0.1.0"
    - requires-python = ">=3.12"
    - dependencies: fastapi>=0.115.0, uvicorn[standard]>=0.30.0, litellm>=1.40.0, pydantic>=2.9.0, pydantic-settings>=2.0.0, python-jose[cryptography]>=3.3.0, python-multipart>=0.0.9, sqlalchemy>=2.0.35, asyncpg>=0.30.0, motor>=3.6.0, neo4j>=5.25.0, hvac>=2.3.0, httpx>=0.27.0, python-dotenv>=1.0.0
    - [project.optional-dependencies]: dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "httpx>=0.27"]
    - [tool.uv]: standard uv configuration

    Keep requirements.txt as a generated artifact (uv pip compile pyproject.toml > requirements.txt) — do NOT delete it yet, but add a comment at the top: "# AUTO-GENERATED by uv — edit pyproject.toml instead"

    Note: pydantic-settings is needed (currently imported in config.py but not in requirements.txt — this was a bug in Phase 1).
  </action>
  <verify>
    <automated>uv sync --dry-run 2>&1 || echo "uv not installed — verify pyproject.toml syntax with: python -c 'import tomllib; tomllib.load(open(\"pyproject.toml\", \"rb\"))'"</automated>
  </verify>
  <done>pyproject.toml is valid TOML, contains all dependencies, uv sync would succeed (or uv not installed but file is valid)</done>
</task>

<task type="auto">
  <name>Task 2: Update Keycloak config and docker-compose for v26</name>
  <files>src/backend/core/config.py, docker-compose.yml, .env.example</files>
  <action>
    Update src/backend/core/config.py Keycloak settings:
    - KEYCLOAK_URL: "https://oauth2.qa.comsatel.com.pe" (was localhost:8080)
    - KEYCLOAK_REALM: "Apps" (was "miaplatform")
    - KEYCLOAK_CLIENT_ID: "miaplatform" (unchanged)
    - Add KEYCLOAK_CLIENT_SECRET: str = "" (new field, required for confidential client)

    Update docker-compose.yml:
    - Add Keycloak v26 service: image quay.io/keycloak/keycloak:26.0
    - Command: start-dev --import-realm
    - Environment: KEYCLOAK_ADMIN, KEYCLOAK_ADMIN_PASSWORD, KC_DB, KC_DB_URL, KC_DB_USERNAME, KC_DB_PASSWORD
    - Ports: 8080:8080
    - Volumes: ./keycloak/realm-import:/opt/keycloak/data/import (for realm config)
    - Note: This is for local dev; production uses oauth2.qa.comsatel.com.pe

    Update .env.example:
    - KEYCLOAK_URL=https://oauth2.qa.comsatel.com.pe
    - KEYCLOAK_REALM=Apps
    - KEYCLOAK_CLIENT_ID=miaplatform
    - KEYCLOAK_CLIENT_SECRET=<from-keycloak-admin-console>
    - Add comments explaining where to find each value
  </action>
  <verify>
    <automated>python -c "from src.backend.core.config import get_settings; s = get_settings(); assert 'oauth2.qa.comsatel.com.pe' in s.KEYCLOAK_URL; assert s.KEYCLOAK_REALM == 'Apps'; print('Keycloak config OK')"</automated>
  </verify>
  <done>Config points to oauth2.qa.comsatel.com.pe, realm is 'Apps', client secret field exists, docker-compose has Keycloak v26 service, .env.example documents all vars</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Keycloak v26 accessibility and configuration</name>
  <files>.env</files>
  <action>
    Human verification steps:
    1. Visit https://oauth2.qa.comsatel.com.pe/admin — verify you can access Keycloak Admin Console
    2. Confirm Realm "Apps" exists in the realm dropdown
    3. Confirm Client "miaplatform" exists under Clients
    4. Copy the Client Secret from Credentials tab
    5. Set KEYCLOAK_CLIENT_SECRET in your .env file
    6. Run: python -c "from src.backend.core.config import get_settings; print(get_settings().KEYCLOAK_URL)" — should print https://oauth2.qa.comsatel.com.pe
  </action>
  <verify>
    <automated>echo "Human verification required — see steps above"</automated>
  </verify>
  <done>Keycloak Admin Console accessible, realm 'Apps' confirmed, client 'miaplatform' exists, client secret saved to .env</done>
</task>

</tasks>

<verification>
- pyproject.toml valid with all dependencies
- Keycloak config points to correct QA host
- docker-compose.yml has Keycloak v26 service
- .env.example documents all required variables
- Human verifies Keycloak accessibility and realm/client existence
</verification>

<success_criteria>
- pyproject.toml replaces requirements.txt as source of truth
- All dependencies from requirements.txt are in pyproject.toml
- pydantic-settings added (was missing in Phase 1)
- KEYCLOAK_URL = https://oauth2.qa.comsatel.com.pe
- KEYCLOAK_REALM = Apps
- KEYCLOAK_CLIENT_ID = miaplatform
- KEYCLOAK_CLIENT_SECRET field exists in config
- docker-compose.yml includes Keycloak v26 for local dev
- Human confirms Keycloak QA host is accessible
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-core-ui-6-8-weeks/02-06-SUMMARY.md`
</output>
