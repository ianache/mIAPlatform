---
phase: 02-agent-core-ui-6-8-weeks
plan: 05
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/frontend/src/pages/ModelRegistryPage.vue
  - src/frontend/src/components/ModelCard.vue
  - src/frontend/src/components/FeatureModelMapping.vue
  - src/frontend/src/components/RegistryActivityTable.vue
  - src/frontend/src/components/RegistryStatsRow.vue
autonomous: true
requirements: [AGENT-04]
user_setup: []

must_haves:
  truths:
    - "User sees global stats: Avg Latency, Cost/1k Tokens, Active Tokens, Registry Health"
    - "User sees registered models list with provider, status, and tags"
    - "User sees feature-to-model mapping panel (Core Chat Agent, Report Generator, Web Search)"
    - "User sees registry activity table with events, model, timestamp, status"
  artifacts:
    - path: "src/frontend/src/pages/ModelRegistryPage.vue"
      provides: "Full model registry page with all 4 sections"
      min_lines: 50
    - path: "src/frontend/src/components/ModelCard.vue"
      provides: "Individual model card with provider logo, status, tags"
      min_lines: 25
    - path: "src/frontend/src/components/FeatureModelMapping.vue"
      provides: "Feature-to-model assignment panel"
      min_lines: 25
    - path: "src/frontend/src/components/RegistryActivityTable.vue"
      provides: "Activity log table"
      min_lines: 25
  key_links:
    - from: "src/frontend/src/components/FeatureModelMapping.vue"
      to: "src/frontend/src/stores/agents.ts"
      via: "reads available agents for assignment"
      pattern: "useAgentStore"
---

<objective>
Build the LLM model registry UI showing registered models, feature mappings, and activity.

Purpose: Users need visibility into which models are available, which features use which models, and registry health.
Output: Model registry page with stats, model cards, feature mapping, and activity table.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (Screen 4: Model Registry)
@.planning/phases/02-agent-core-ui-6-8-weeks/02-02-PLAN.md (layout, types, API client)
@src/frontend/src/types/index.ts
@tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create model registry components</name>
  <files>src/frontend/src/components/ModelCard.vue, src/frontend/src/components/FeatureModelMapping.vue, src/frontend/src/components/RegistryActivityTable.vue, src/frontend/src/components/RegistryStatsRow.vue</files>
  <action>
    Create src/frontend/src/components/RegistryStatsRow.vue:
    - 4 stat cards: Avg Latency (placeholder "245ms"), Cost/1k Tokens (placeholder "$0.003"), Active Tokens (placeholder "12"), Registry Health (placeholder "98%" with green indicator)
    - Same pattern as StatsRow from Plan 04 but different stats
    - Glass effect container, Space Grotesk values in primary color

    Create src/frontend/src/components/ModelCard.vue:
    - Card with glass bg, provider logo placeholder (colored circle with provider initial: O for OpenAI, A for Anthropic, G for Google)
    - Model name (Space Grotesk), provider name below
    - Status badge: "Active" (green), "Deprecated" (amber), "Beta" (primary)
    - Tags: pill-shaped badges in surface.highest bg with onSurface.variant text (e.g., "vision", "function-calling", "128k context")
    - Hover: bg shift to surface.high (no borders)

    Create src/frontend/src/components/FeatureModelMapping.vue:
    - Panel title "Feature-to-Model Mapping"
    - 3 rows: Core Chat Agent, Report Generator, Web Search Assistant
    - Each row: feature name (left), model dropdown (right, populated from available agents/models), save indicator
    - Dropdown shows current assigned model, click to change
    - Use surface.low bg for rows, alternating with surface for visual separation (no borders)

    Create src/frontend/src/components/RegistryActivityTable.vue:
    - Table with columns: Event, Model, Timestamp, Status
    - Placeholder data: "Model Added", "Temperature Updated", "Model Deprecated", etc.
    - Status column: colored dot + text (green=success, amber=warning)
    - Table bg: surface container, rows alternate surface.low/surface (no borders between rows — use bg shift)
    - Max 10 rows, scrollable if more
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>All components compile, model cards render with provider info, feature mapping shows 3 rows, activity table displays placeholder data</done>
</task>

<task type="auto">
  <name>Task 2: Assemble model registry page</name>
  <files>src/frontend/src/pages/ModelRegistryPage.vue</files>
  <action>
    Create src/frontend/src/pages/ModelRegistryPage.vue:
    - Page title "Model Registry" in Space Grotesk, primary color
    - RegistryStatsRow at top
    - Two-column layout on lg+: left (2/3) = Registered Models list with ModelCard grid, right (1/3) = FeatureModelMapping panel
    - Below: RegistryActivityTable full width
    - Models list: hardcoded placeholder data for Phase 2 (will connect to backend in Phase 3):
      - OpenAI: gpt-4o (Active, tags: vision, function-calling, 128k), gpt-4o-mini (Active, tags: fast, 128k), gpt-3.5-turbo (Deprecated, tags: fast, 16k)
      - Anthropic: claude-3-5-sonnet (Active, tags: reasoning, 200k), claude-3-opus (Active, tags: reasoning, 200k), claude-3-haiku (Active, tags: fast, 200k)
      - Google: gemini-1.5-pro (Active, tags: multimodal, 1M), gemini-1.5-flash (Active, tags: fast, 1M)
    - Glass effect cards throughout, no borders (no-line rule)
    - Responsive: single column on mobile
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Model registry page renders with all 4 sections, 9 model cards displayed, feature mapping shows 3 features, activity table has placeholder events, build succeeds</done>
</task>

</tasks>

<verification>
- Model registry page shows 4 sections: stats, models, feature mapping, activity
- 9 model cards rendered with correct provider, status, and tags
- Feature-to-model mapping shows 3 features with dropdowns
- Activity table displays placeholder events
- All components use design system styling
- Responsive layout: 2-col on lg, 1-col on mobile
</verification>

<success_criteria>
- Page renders with RegistryStatsRow, model grid, feature mapping, activity table
- Model cards show provider logos, status badges, and tags
- Feature mapping panel allows model selection per feature (UI only, no backend yet)
- `npm run build` succeeds
- Dark theme, glassmorphism, no borders throughout
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-core-ui-6-8-weeks/02-05-SUMMARY.md`
</output>
