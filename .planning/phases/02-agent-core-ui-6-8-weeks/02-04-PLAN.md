---
phase: 02-agent-core-ui-6-8-weeks
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/frontend/src/pages/AgentManagementPage.vue
  - src/frontend/src/pages/SettingsPage.vue
  - src/frontend/src/components/AgentCard.vue
  - src/frontend/src/components/APIKeyManager.vue
  - src/frontend/src/components/StatsRow.vue
autonomous: true
requirements: [AGENT-02, AGENT-03]
user_setup: []

must_haves:
  truths:
    - "User sees a list of agent cards with name, status, and model"
    - "User can click Edit to navigate to agent edit form"
    - "User can see API Keys section with provider keys and validation status"
    - "User can see stats: Total Tokens, Avg Latency, Success Rate, Current Spend"
    - "Settings page has API key management with masked key display"
  artifacts:
    - path: "src/frontend/src/pages/AgentManagementPage.vue"
      provides: "Agent list with cards and edit/delete actions"
      min_lines: 40
    - path: "src/frontend/src/pages/SettingsPage.vue"
      provides: "Settings with API key management and stats"
      min_lines: 40
    - path: "src/frontend/src/components/AgentCard.vue"
      provides: "Individual agent card with status indicator and actions"
      min_lines: 30
    - path: "src/frontend/src/components/APIKeyManager.vue"
      provides: "API key input, validation, masked display per provider"
      min_lines: 30
  key_links:
    - from: "src/frontend/src/pages/AgentManagementPage.vue"
      to: "src/frontend/src/stores/agents.ts"
      via: "useAgentStore().fetchAgents() on mount"
      pattern: "fetchAgents"
    - from: "src/frontend/src/components/AgentCard.vue"
      to: "src/frontend/src/router/index.ts"
      via: "router.push(`/agents/${agent.id}/edit`)"
      pattern: "router\\.push"
---

<objective>
Build agent management list UI and settings page with API key management.

Purpose: Users need to view, edit, and manage their agents after creation, and configure LLM provider API keys.
Output: Agent list page with card grid, settings page with API key manager and stats.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (Screen 3: Agent Management / Settings)
@.planning/phases/02-agent-core-ui-6-8-weeks/02-01-PLAN.md (agent schema contract)
@.planning/phases/02-agent-core-ui-6-8-weeks/02-02-PLAN.md (layout, types, API client)
@.planning/phases/02-agent-core-ui-6-8-weeks/02-03-PLAN.md (agent creation form patterns)
@src/frontend/src/types/index.ts
@src/frontend/src/stores/agents.ts
@tailwind.config.js
</context>

<interfaces>
<!-- Key types from Plan 02 -->
```typescript
interface Agent {
  id: string; name: string; description?: string; avatar_url?: string;
  provider: 'openai' | 'anthropic' | 'google' | 'other';
  model: string; temperature: number; system_prompt?: string;
  capabilities: string[]; status: 'ready' | 'calibrating' | 'disabled';
  created_at: string; updated_at: string;
}

interface APIKey {
  id: string; provider: string; key_masked: string;
  is_valid: boolean; last_validated: string;
}
```

From src/frontend/src/stores/agents.ts (created by Plan 03):
```typescript
function fetchAgents(): Promise<void>
function deleteAgent(id: string): Promise<void>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build agent list page with card components</name>
  <files>src/frontend/src/pages/AgentManagementPage.vue, src/frontend/src/components/AgentCard.vue, src/frontend/src/components/StatsRow.vue</files>
  <action>
    Create src/frontend/src/components/StatsRow.vue:
    - Horizontal row of 4 stat cards: Total Tokens, Avg Latency, Success Rate, Current Spend
    - Each stat: label (Inter, onSurface.variant), value (Space Grotesk, primary color, larger)
    - Glass effect container (backdrop-blur-glass), evenly spaced with flex
    - Placeholder values for now (will connect to analytics in Phase 4)

    Create src/frontend/src/components/AgentCard.vue:
    - Card with glass effect bg, rounded corners, hover state (bg shifts to surface.high — no borders)
    - Top row: avatar circle (40px) + agent name (Space Grotesk) + status badge (green dot for "ready", amber for "calibrating", gray for "disabled")
    - Middle: model name in onSurface.variant (e.g., "GPT-4o · OpenAI")
    - Bottom: capability count ("3 capabilities") + Edit button (primary text, no bg)
    - Click Edit → router.push(`/agents/${agent.id}/edit`)
    - Hover: subtle bg shift to surface.high (no border, no-line rule)

    Create src/frontend/src/pages/AgentManagementPage.vue:
    - Page title "Agents" in Space Grotesk, primary color
    - "New Agent" button (primary gradient) → router.push('/agents/new')
    - StatsRow at top
    - Agent grid: 3 columns on lg, 2 on md, 1 on mobile
    - On mount: call useAgentStore().fetchAgents()
    - Empty state: "No agents yet" with illustration placeholder + "Create your first agent" button
    - Loading state: skeleton cards (pulsing bg in surface.low)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Agent list page renders with cards grid, fetches agents from API on mount, empty/loading states work</done>
</task>

<task type="auto">
  <name>Task 2: Build settings page with API key manager</name>
  <files>src/frontend/src/pages/SettingsPage.vue, src/frontend/src/components/APIKeyManager.vue</files>
  <action>
    Create src/frontend/src/components/APIKeyManager.vue:
    - Section per provider: OpenAI, Anthropic, Google Gemini
    - Each section: provider name + logo placeholder, API key input (password type, masked after save)
    - Save button per provider (primary text), Validate button (outline style)
    - Validation status indicator: green checkmark (valid), red X (invalid), gray dot (not tested)
    - Last validated timestamp in onSurface.variant
    - On save: store key in state (no backend endpoint yet — placeholder for Phase 1 Vault integration)
    - On validate: placeholder call (will connect to backend validation endpoint)

    Create src/frontend/src/pages/SettingsPage.vue:
    - Page title "Settings" in Space Grotesk, primary color
    - Two sections: "API Keys" (APIKeyManager component) and "Platform Stats" (StatsRow reused)
    - Stats section: Total Tokens, Avg Latency, Success Rate, Current Spend (placeholder values)
    - Layout: single column, max-width 800px, centered
    - Glass effect cards for each section
    - Note: API keys are stored client-side for now (Phase 5 will add Vault integration for secure storage)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Settings page renders with API key manager for 3 providers, stats row displays, build succeeds</done>
</task>

</tasks>

<verification>
- Agent management page lists agents in responsive grid
- Agent cards show name, status badge, model, edit action
- Empty state and loading skeletons render correctly
- Settings page has API key sections for OpenAI, Anthropic, Google Gemini
- API key inputs mask values after save
- Validation status indicators show correct state
- Stats row displays placeholder values
- All components use design system: dark theme, glassmorphism, no borders
</verification>

<success_criteria>
- Agent list fetches from GET /api/v1/agents on mount
- Cards display agent name, status, model with correct styling
- Edit button navigates to agent edit route
- Settings page shows API key manager with 3 provider sections
- `npm run build` succeeds
- Responsive grid: 3 cols → 2 cols → 1 col
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-core-ui-6-8-weeks/02-04-SUMMARY.md`
</output>
