---
phase: 02-agent-core-ui-6-8-weeks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/api/agents.py
  - src/backend/api/__init__.py
  - src/backend/core/config.py
  - src/backend/main.py
  - src/backend/models/agent.py
  - src/backend/models/__init__.py
  - src/backend/models/agent_schemas.py
autonomous: true
requirements: [AGENT-01]
user_setup: []

must_haves:
  truths:
    - "POST /api/v1/agents creates an agent and returns its definition"
    - "GET /api/v1/agents lists all agents for the tenant"
    - "GET /api/v1/agents/{id} returns a single agent's full config"
    - "PUT/PATCH /api/v1/agents/{id} updates agent fields"
    - "DELETE /api/v1/agents/{id} removes an agent"
    - "Agent schema includes identity, model config, role, capabilities"
  artifacts:
    - path: "src/backend/models/agent.py"
      provides: "SQLAlchemy Agent model with all fields"
      contains: "class Agent"
    - path: "src/backend/models/agent_schemas.py"
      provides: "Pydantic request/response schemas"
      exports: ["AgentCreate", "AgentUpdate", "AgentResponse", "CapabilityToggle"]
    - path: "src/backend/api/agents.py"
      provides: "CRUD routes for agents"
      exports: ["router"]
  key_links:
    - from: "src/backend/api/agents.py"
      to: "src/backend/models/agent.py"
      via: "SQLAlchemy ORM operations"
      pattern: "db\\.(add|query|delete)"
    - from: "src/backend/api/agents.py"
      to: "src/backend/core/security.py"
      via: "JWT verification dependency"
      pattern: "Depends\\(security\\)"
---

<objective>
Create the agent definition schema and full CRUD API for user-defined agents.

Purpose: Every UI plan (03, 04, 05) depends on these endpoints existing. This is the foundation contract.
Output: Agent SQLAlchemy model, Pydantic schemas, CRUD API routes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/backend/main.py
@src/backend/core/security.py
@src/backend/db/database.py
@src/backend/api/tenants.py
@src/backend/core/config.py
@requirements.txt
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/backend/db/database.py:
```python
Base = declarative_base()
async def get_db() -> AsyncGenerator[AsyncSession, None]
```

From src/backend/core/security.py:
```python
security = HTTPBearer()
def verify_token(credentials: HTTPAuthorizationCredentials) -> dict
def create_access_token(data: dict, expires_delta: Optional[timedelta] = None) -> str
```

From src/backend/api/tenants.py (pattern to follow):
```python
router = APIRouter(prefix="/tenants", tags=["tenants"])
# Uses Depends(security) for auth, Depends(get_db) for DB session
```

From src/backend/core/config.py:
```python
class Settings(BaseSettings):
    # JWT, DB, Keycloak, Vault settings
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create agent SQLAlchemy model and Pydantic schemas</name>
  <files>src/backend/models/__init__.py, src/backend/models/agent.py, src/backend/models/agent_schemas.py</files>
  <behavior>
    - Test 1: AgentCreate with valid data (name, description, avatar_url, provider, model, temperature, system_prompt, capabilities) produces valid AgentResponse
    - Test 2: AgentCreate with missing required name field fails validation
    - Test 3: CapabilityToggle with valid capability names (web_search, file_analysis, code_execution) serializes correctly
    - Test 4: AgentUpdate with partial data only modifies provided fields
  </behavior>
  <action>
    Create src/backend/models/agent.py with SQLAlchemy model:
    - id: UUID (primary key, default uuid4)
    - tenant_id: String (FK to tenants, indexed for isolation)
    - name: String (required, 3-100 chars)
    - description: Text (optional)
    - avatar_url: String (optional)
    - provider: String (enum: openai, anthropic, google, other)
    - model: String (required, e.g. "gpt-4", "claude-3-sonnet")
    - temperature: Float (0.0-2.0, default 0.7)
    - system_prompt: Text (optional, for role definition)
    - capabilities: JSON column (array of enabled capability strings)
    - status: String enum (ready, calibrating, disabled) default "ready"
    - created_at, updated_at: DateTime defaults

    Create src/backend/models/agent_schemas.py with Pydantic v2 schemas:
    - AgentCreate: name (required, 3-100 chars), description, avatar_url, provider (enum), model, temperature (0-2, default 0.7), system_prompt, capabilities (list of strings, default empty), status (default "ready")
    - AgentUpdate: all fields optional
    - AgentResponse: mirrors Agent model with all fields, id as string
    - AgentListResponse: list of AgentResponse with total count
    - CapabilityToggle: capability_name (string), enabled (bool)

    Create src/backend/models/__init__.py exporting all models and schemas.

    Write tests in tests/test_agent_schemas.py validating schema behavior before implementation (TDD).
  </action>
  <verify>
    <automated>pytest tests/test_agent_schemas.py -x</automated>
  </verify>
  <done>Agent model and schemas exist, all schema tests pass, types are exported</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement agent CRUD API routes</name>
  <files>src/backend/api/agents.py</files>
  <behavior>
    - Test 1: POST /api/v1/agents with valid AgentCreate returns 201 with AgentResponse
    - Test 2: POST /api/v1/agents with duplicate name returns 409
    - Test 3: GET /api/v1/agents returns list filtered by tenant_id
    - Test 4: GET /api/v1/agents/{id} returns 200 or 404
    - Test 5: PATCH /api/v1/agents/{id} updates only provided fields
    - Test 6: DELETE /api/v1/agents/{id} returns 204 and agent is gone
    - Test 7: All endpoints require valid JWT (401 without token)
    - Test 8: Agents are tenant-isolated (tenant A cannot see tenant B's agents)
  </behavior>
  <action>
    Create src/backend/api/agents.py following the pattern from tenants.py:
    - POST /api/v1/agents: Creates agent, validates name uniqueness per tenant, returns 201
    - GET /api/v1/agents: Lists agents filtered by tenant_id from JWT, returns list with count
    - GET /api/v1/agents/{agent_id}: Returns single agent, 404 if not found, 403 if wrong tenant
    - PATCH /api/v1/agents/{agent_id}: Partial update, validates name uniqueness if changed
    - DELETE /api/v1/agents/{agent_id}: Soft delete (set status="disabled") or hard delete, returns 204

    All endpoints use Depends(security) for JWT auth and extract tenant_id from token payload.
    Tenant isolation: every query includes tenant_id filter from JWT claims.
    Register the router in src/backend/main.py with app.include_router(agents.router, prefix="/api/v1").
  </action>
  <verify>
    <automated>pytest tests/test_agents_api.py -x</automated>
  </verify>
  <done>All CRUD endpoints work, tenant isolation verified, auth required on all routes</done>
</task>

</tasks>

<verification>
- Agent model exists in src/backend/models/agent.py with all required fields
- Agent schemas validate correctly (Pydantic)
- All 5 CRUD operations work via API
- Tenant isolation enforced on every endpoint
- Router registered in main.py
- Tests pass for both schemas and API
</verification>

<success_criteria>
- POST /api/v1/agents creates agent with full config, returns 201
- GET /api/v1/agents returns tenant-scoped list
- Agent schema matches Phase 3 orchestration needs (provider, model, temperature, system_prompt, capabilities)
- No endpoint accessible without valid JWT
- Tenant A cannot access Tenant B's agents
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-core-ui-6-8-weeks/02-01-SUMMARY.md`
</output>
