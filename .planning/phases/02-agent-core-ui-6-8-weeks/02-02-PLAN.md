---
phase: 02-agent-core-ui-6-8-weeks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/main.ts
  - src/frontend/src/App.vue
  - src/frontend/src/router/index.ts
  - src/frontend/src/stores/auth.ts
  - src/frontend/src/layouts/DashboardLayout.vue
  - src/frontend/src/components/Sidebar.vue
  - src/frontend/src/components/TopBar.vue
  - src/frontend/src/styles/globals.css
  - src/frontend/src/types/index.ts
  - src/frontend/src/api/client.ts
  - src/frontend/vite.config.ts
  - src/frontend/tsconfig.json
  - src/frontend/index.html
autonomous: true
requirements: [AGENT-02]
user_setup: []

must_haves:
  truths:
    - "App renders with dark theme (#091421 background)"
    - "Sidebar shows 5 nav items: Workspace, Agents, Library, Analytics, Settings"
    - "Top bar renders with search placeholder"
    - "Routes navigate between pages without full reload"
    - "API client configured with base URL and auth header injection"
    - "TypeScript types for Agent, LLM Provider, API Key defined"
  artifacts:
    - path: "src/frontend/src/main.ts"
      provides: "Vue app entry with Pinia, Router"
      contains: "createApp"
    - path: "src/frontend/src/router/index.ts"
      provides: "Route definitions for all Phase 2 pages"
      exports: ["router"]
    - path: "src/frontend/src/layouts/DashboardLayout.vue"
      provides: "Shell with sidebar + topbar + content slot"
      min_lines: 40
    - path: "src/frontend/src/types/index.ts"
      provides: "TypeScript interfaces for Agent, Provider, APIKey"
      exports: ["Agent", "AgentCreate", "LLMProvider", "APIKey"]
    - path: "src/frontend/src/api/client.ts"
      provides: "Axios/fetch instance with auth interceptor"
      exports: ["apiClient"]
  key_links:
    - from: "src/frontend/src/api/client.ts"
      to: "src/backend/api/agents.py"
      via: "HTTP interceptor attaching JWT from auth store"
      pattern: "Authorization.*Bearer"
    - from: "src/frontend/src/layouts/DashboardLayout.vue"
      to: "src/frontend/src/router/index.ts"
      via: "router-view in content slot"
      pattern: "router-view"
---

<objective>
Create Vue 3 application scaffold with routing, layout shell, type definitions, and API client.

Purpose: All UI plans (03, 04, 05) need this foundation — types, routing, layout, API client. This is the contract layer.
Output: Working Vue 3 SPA with dark theme, sidebar layout, typed API client, route placeholders.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/REQUIREMENTS.md (Design System Specification section)
@tailwind.config.js
@package.json
@src/frontend/index.html
@src/backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write interface contracts and API client</name>
  <files>src/frontend/src/types/index.ts, src/frontend/src/api/client.ts, src/frontend/src/stores/auth.ts</files>
  <action>
    Create src/frontend/src/types/index.ts with TypeScript interfaces:
    - Agent: { id, tenant_id, name, description, avatar_url, provider, model, temperature, system_prompt, capabilities: string[], status, created_at, updated_at }
    - AgentCreate: Omit id, tenant_id, created_at, updated_at from Agent, all required except description/avatar_url/system_prompt
    - LLMProvider: { id: 'openai' | 'anthropic' | 'google' | 'other', name: string, logo_url: string }
    - APIKey: { id, provider, key_masked: string, is_valid: boolean, last_validated: string }
    - ApiResponse<T>: { data: T, total?: number }
    - ApiError: { detail: string, status_code: number }

    Create src/frontend/src/api/client.ts:
    - Use fetch (no extra dependency) with base URL from env VITE_API_URL (default http://localhost:8000)
    - Auth interceptor: reads token from localStorage, adds Authorization: Bearer header
    - Typed request methods: get<T>, post<T>, patch<T>, delete<T>
    - Error handling: parse JSON error responses, throw typed ApiError

    Create src/frontend/src/stores/auth.ts (Pinia store):
    - State: token (string | null), user (object | null)
    - Actions: setToken(token), logout(), isAuthenticated (getter)
    - Token persistence: localStorage with key 'orchestra_token'
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>TypeScript compiles without errors, types exported, API client has typed methods, auth store works</done>
</task>

<task type="auto">
  <name>Task 2: Build app entry, router, and dashboard layout shell</name>
  <files>src/frontend/src/main.ts, src/frontend/src/App.vue, src/frontend/src/router/index.ts, src/frontend/src/layouts/DashboardLayout.vue, src/frontend/src/components/Sidebar.vue, src/frontend/src/components/TopBar.vue, src/frontend/src/styles/globals.css, src/frontend/vite.config.ts, src/frontend/tsconfig.json, src/frontend/index.html</files>
  <action>
    Update src/frontend/index.html: Add Space Grotesk, Manrope, Inter Google Fonts links, set dark background on body.

    Create src/frontend/src/styles/globals.css:
    - Import Tailwind directives
    - Set body bg to #091421, text to #D9E3F6
    - Apply font-body (Manrope) globally
    - Apply glassmorphism utility: .glass { background: rgba(22, 32, 46, 0.6); backdrop-filter: blur(12px); }
    - No-line rule: no 1px borders, use bg shifts instead

    Create src/frontend/src/router/index.ts with routes:
    - / → DashboardLayout → placeholder Workspace page
    - /agents → DashboardLayout → placeholder Agents page
    - /agents/new → DashboardLayout → Agent Creation page (placeholder for Plan 03)
    - /agents/:id/edit → DashboardLayout → Agent Edit page
    - /library → DashboardLayout → placeholder Model Registry page (placeholder for Plan 05)
    - /analytics → DashboardLayout → placeholder Analytics page
    - /settings → DashboardLayout → placeholder Settings page (placeholder for Plan 04)

    Create src/frontend/src/layouts/DashboardLayout.vue:
    - Left sidebar (240px fixed): uses Sidebar component
    - Top bar (sticky): uses TopBar component
    - Main content area: <router-view /> with padding
    - Dark background #091421

    Create src/frontend/src/components/Sidebar.vue:
    - Fixed left, 240px width, bg surface.low (#121C2A)
    - Logo: "Orchestra" in Space Grotesk, primary color
    - Nav items: Workspace (/), Agents (/agents), Library (/library), Analytics (/analytics), Settings (/settings)
    - Active state: primary gradient background, no border (no-line rule)
    - Use vue-router's useRoute for active detection

    Create src/frontend/src/components/TopBar.vue:
    - Sticky top, glass effect (backdrop-blur-glass)
    - Search input (placeholder "Search agents, models...") with outline color, no border
    - Right side: user avatar placeholder

    Create src/frontend/src/main.ts: Initialize Vue app with Pinia and Router, mount to #app.
    Create src/frontend/src/App.vue: Simple wrapper with <router-view />.

    Configure vite.config.ts for Vue 3, alias @ to src/.
    Configure tsconfig.json for strict TypeScript with Vue support.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>npm run build succeeds, layout renders with dark theme, sidebar has 5 nav items, top bar has search, routes navigate without reload</done>
</task>

</tasks>

<verification>
- TypeScript types compile without errors
- Vite build succeeds
- App renders with #091421 background
- Sidebar has all 5 navigation items with correct routes
- Top bar renders with search input
- API client configured with auth interceptor
- Auth store persists token in localStorage
- Glassmorphism utility class works
- No 1px borders visible (no-line rule)
</verification>

<success_criteria>
- `npm run build` completes without errors
- All 5 routes defined and navigable
- Layout matches design system: dark theme, Space Grotesk headlines, Manrope body, glassmorphism
- Sidebar active state uses primary gradient (no borders)
- API client attaches JWT to all requests automatically
- Types match backend Agent model from Plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-core-ui-6-8-weeks/02-02-SUMMARY.md`
</output>
